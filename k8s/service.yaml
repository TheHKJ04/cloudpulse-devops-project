apiVersion: v1
kind: Service

metadata:
  name: cloudpulse-service
  namespace: cloudpulse

spec:
  type: NodePort

  selector:
    app: cloudpulse

  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080